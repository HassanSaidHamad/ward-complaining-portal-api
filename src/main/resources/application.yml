server:
  port: 9095

spring:
  application:
    name: ward-complaining-portal

  datasource:
    url: jdbc:postgresql://localhost:5432/ward_db
    username: postgres
    password: never123%
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

app:
  jwtSecret: "carrentalappSuperSecretKeyThatIsExtremelyLongAndSecureForHS512Signing1234567890"
  jwtExpirationMs: 86400000  # 24 hours



